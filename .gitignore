# 凭据与环境
.env.local
.env
wechat_config.json

# 系统文件
.DS_Store

# 工作区本地数据（不进入共有仓库）
.obsidian/
.workbuddy/

# 编译中间产物（-g 生成的调试符号、临时数据）
*.dSYM
*.o

# 草稿状态记录（含 draft_id，属本地上传记录，不入库）
articles/**/wechat-draft.json
