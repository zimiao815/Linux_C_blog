# `fcntl`：文件打开后，如何控制一个 fd？

- 核心问题：文件已经打开后，如何在不重新 `open` 的情况下安全地修改 fd 的行为？
- 一句话结论：用 `fcntl` 读取或修改打开文件状态、复制带 close-on-exec 的 fd，并按需申请建议性记录锁；不同命令操作的对象层次不同，必须核对返回值和资源生命周期。
- 适用范围：POSIX/Linux 文件描述符语义；实验在 macOS 14.8.8、Apple clang 16.0.0 验证。
- 不处理：设备控制接口、分布式锁、强制锁和数据库事务。
